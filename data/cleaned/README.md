# Cleaned Data

This folder contains cleaned and analysis-ready datasets generated from the original CMS Medicare Inpatient Hospital data.

The cleaned datasets will be created through the Python data preparation and validation workflow.

Raw CMS source files are preserved separately in the `data/raw` folder.
